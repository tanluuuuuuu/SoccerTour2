import React, { ElementType } from 'react';
export interface FormResetProps<TAs extends ElementType = any> {
    as?: TAs;
    onClick?: (...args: any[]) => any;
}
/**
 * A Form reset button
 *
 * @memberof Form
 */
declare function Reset<TAs extends ElementType = 'button'>(props: FormResetProps<TAs> & Omit<React.ComponentPropsWithoutRef<TAs>, 'as' | 'triggers'>): JSX.Element;
export default Reset;
